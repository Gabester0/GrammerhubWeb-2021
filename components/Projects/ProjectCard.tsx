import React from 'react';

const ProjectCard = (props)=>{
// Nest separate contributors elements
// Nest separate C
    return (
        <div>
            <h3>{props.name}</h3>
        </div>
    )
}

export default ProjectCard;